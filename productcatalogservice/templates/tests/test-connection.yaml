apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "productcategoryservice.fullname" . }}-test-connection"
  labels:
    {{- include "productcategoryservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "productcategoryservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
